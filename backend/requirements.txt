pydantic>=2.0
PyYAML>=6.0
ruamel.yaml>=0.18
shapely>=2.0
ezdxf>=1.0
numpy>=1.24
trimesh>=4.0
# trimesh needs a graph engine to split a mesh into separate bodies. Without
# one, measuring a vendor model dies with "no graph engines available" and the
# catalogue import is dead on arrival. networkx is the light way to have one:
# pure Python, where the alternative (scipy) is forty megabytes.
networkx>=3.0
fastapi>=0.110
uvicorn[standard]>=0.27
pytest>=8.0
httpx>=0.27          # only for the API tests (starlette TestClient)
